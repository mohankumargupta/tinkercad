---
title: Reverse Engineering Existing Tinkercad designs
description: Reverse engineer tinkercad
sidebar:
  order: 5
---

import { Image } from 'astro:assets';
import poster from '../../../assets/reverse-engineer-existing-tinkercad.png';
import forkimage from '../../../assets/fork.png';

import HyperLink from '../../../components/HyperLink.astro';

<Image src={poster} alt="Reverse engineer tinkercad" />

## Sequence

1. Select object
2. Press Ctrl-D to duplicate or hit duplicate icon
3. move item away horizontally(use shift+arrow keys)
4. **Then ungroup this object using ungroup icon, then click off(click on empty space)**
5. Select the duplicate again, and move item away using shift+arrow keys.
   You will then see one of the consituent parts move away. You can move way other consituent parts also.
   
   
   There are two options from here:
    - if it is a basic shape, you are done breaking this down to its simplest,
      repeat the process for other shapes
    - if it is a union of multiple shapes(you can tell by observing if ungroup icon is enabled or greyed out),
      then repeat step 5 for this selected object.

## Finding dimensions

Simply put a ruler on the workplane, then select an object to find its dimensions.

## Where to get existing tinkercad designs

First, make sure you are logged in to tinkercad.com .

For example, if you were looking to design a fork, go to  <HyperLink href="https://images.google.com">images.google.com</HyperLink> in your browser and type 

```text
fork site:tinkercad.com
```

Find one you are interested in, select it and press Copy and Tinker button

<Image src={forkimage} alt="fork" />